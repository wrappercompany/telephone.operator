[project]
name = "telephone-operator"
version = "0.1.0"
description = "Telephone operator project"
requires-python = ">=3.12"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/telephone_operator"] 